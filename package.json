{
  "name": "@mawhea/module-winston-logger",
  "version": "1.0.0",
  "description": "Wrapper class for npm package winston to format log messages for AWS CloudWatch.",
  "main": "logger.js",
  "scripts": {
    "test": "mocha test/ --recursive",
    "test:coverage": "nyc --reporter=html --reporter=text --report-dir=.coverage mocha test/*test.js --recursive"
  },
  "keywords": [
    "winston",
    "logger"
  ],
  "dependencies": {
    "traverse": "^0.6.6",
    "winston": "3.1.0"
  },
  "author": "Matthew Wheatley",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^6.0.2",
    "nyc": "^14.1.0"
  },
  "files": [
    "logger.js",
    "config/",
    "lib/"
  ]
}
